what about a cli that connects helix & zellij through clipboard, then sending
selected text to the active pane, presumably a repl
we could keep a server running with the same architecture of twins that we used for hypnos
likewise, it could evolve into a zellij plugin properly if it is possible to choose the pane explicitly

- use process substitution to log installation & failures => install > >(less) 2> >(tee errors.txt)

- update language configs helix: cmake, docker-compose, javascript, latex, nu, python, scala
- add language support for kdl & edn
- todor completions?

document niri installation => ubuntu

kanata:
;; TODO: create aliases header to embed hotkeys automatically => source a file with the bind definitions that will be forged by embedding
;; TOOD: pass terminal sequences as aliases & leave the topographic bindings in place
;; TODO: possible to use grave key as layer
;; TODO: bind pasting to key directly
;; TODO: clean up comments & outdated

.justfile
34:# TODO: change to bash for compatibility, however this does not produce colored logging
explicitly run just scripts in bash; for this, update color output to be compatible

.just/config.sh
67:  # TODO: double check linux config

==================================================
ex-situ/bpython/alacritty.theme
  line 13     TODO   update all colors
ex-situ/shell/nu/keymap.nu
  line 28     BUG    `shells` & `dirs` commands not present. plugins?
  line 83     BUG    are they useful commands?
in-silico/pkg_manager.sh
  line 114    TODO   decide & document: starship, nushell & nerd-fonts
install.sh
  line 13     TODO   document nerd font installation; present on shell history for ubuntu
  line 44     BUG    ubuntu errors out on mongodb for some reason
==================================================
