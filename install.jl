####################################################################################################

archDir = string(homedir(), "/", ".archive")
setupDir = string(archDir, "/", ".just", "/", ".setup")

include(string(setupDir, "/", "julia.jl"))

####################################################################################################

import Pkg

for p in packages
  @info p
  Pkg.add(p)
end

####################################################################################################
