(defcfg
  process-unmapped-keys yes
)

;; Only three keys in defsrc for testing
(defsrc)

(deflayer base
  ;; O-a → Alt+F1
  (combo lalt a) (alt f1)

  ;; O-c → Alt+F4
  (combo lalt c) (alt f4)

  ;; filler so deflayer has same length as defsrc
  ___
)
